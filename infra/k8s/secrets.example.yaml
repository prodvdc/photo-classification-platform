apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
type: Opaque
stringData:
  database_url: postgresql+psycopg2://postgres:postgres@photo-postgres:5432/photo_platform
  jwt_secret: change-me
  admin_email: admin@example.com
  admin_password: admin123
  db_user: postgres
  db_password: postgres
